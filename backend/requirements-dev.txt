# Local-only dependencies for the LightGBM forecasting benchmark.
# NOT installed in the production image — the /forecasting/sku/{sku}/compare
# endpoint is gated behind the LGBM_BENCHMARK env flag and only mounts when
# these packages are importable.
#
# Install locally with:
#   pip install -r requirements-dev.txt

-r requirements.txt

lightgbm>=4.0
scikit-learn>=1.3
