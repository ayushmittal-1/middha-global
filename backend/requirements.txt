# Pinned per the production-readiness audit (C5). All versions match what
# was actually verified working in the backend/.venv at pin time. Bumps
# must go through dependency PRs so vulnerability scanning + upgrade
# testing happen deliberately, not on every fresh build.
fastapi==0.136.3
uvicorn[standard]==0.48.0
httpx==0.28.1
groq==1.2.0
python-dotenv==1.2.2
aiosqlite==0.22.1
playwright==1.60.0
motor==3.7.1
PyJWT==2.13.0
bcrypt==5.0.0
cryptography==49.0.0
APScheduler==3.11.2
# pandas pinned to 2.3.3 (was 3.0.3) — gluonts 0.17 doesn't support
# pandas 3.x yet. Downgrade is safe: the codebase's pandas usage is
# 2.x/3.x compatible, verified against the local bench + refresh harness
# on 2.3.3 before this change shipped. Revisit when gluonts adds
# pandas-3 support.
pandas==2.3.3
numpy==2.5.0
prophet==1.3.0
# Multi-model forecast picker candidates. LightGBM + XGBoost are the
# tree-based baselines the nightly cache job bakes off against Prophet
# + naive; scikit-learn is a shared dep. Winner-per-SKU lands in
# forecast_cache and drives the restock tab.
lightgbm==4.7.0
xgboost==3.4.0
scikit-learn==1.9.0
# Deep-learning candidates (DeepAR + Temporal Fusion Transformer via
# gluonts). Heavy: torch + pytorch-lightning add ~1.5 GB to the image
# and need ~2 GB RAM at model-load time. Gated behind the DEEPTS_ENABLED
# env flag — nothing imports these at startup unless the flag is set,
# so a service that doesn't want the RAM overhead can simply leave the
# env unset. Instance sizing note: needs Render Pro Plus (8 GB) minimum
# to run DeepAR/TFT training inside the same process; Standard (2 GB)
# will OOM even at import time.
gluonts[torch]==0.17.0
pytorch-lightning==2.6.5
tzdata==2025.2
beautifulsoup4==4.15.0
lxml==6.1.1
# C2 — inbound rate limiting (slowapi, in-process; upgrade to a Redis
# backend later when moving to multi-instance).
slowapi==0.1.10
