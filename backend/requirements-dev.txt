# Local-only supplemental dependencies. All forecasting model libraries
# (lightgbm, xgboost, gluonts[torch], pytorch-lightning) have been
# promoted to requirements.txt because production runs them when the
# LGBM_BENCHMARK / DEEPTS_ENABLED env flags are on. This file exists
# for any future dev-only tooling (test runners, profilers, notebooks)
# that shouldn't ship to prod.
#
# Install locally with:
#   pip install -r requirements-dev.txt

-r requirements.txt

# The test runner. Was previously undeclared and only present in local
# virtualenvs, so CI failed with "pytest: command not found" the first time
# the suite ran anywhere but a developer's machine.
pytest==9.1.1
